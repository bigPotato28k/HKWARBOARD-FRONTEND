import { Suspense } from "react";
import { ResetPasswordForm } from "@/features/auth-forms";
export default function ResetPasswordLegacyPage(){return <Suspense fallback={null}><ResetPasswordForm/></Suspense>;}
